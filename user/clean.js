const diff_cleanupMerge = require('../workbench/diff-workbench');

describe('clean', function () {
  describe('a - b', function () {
    it('delete', function () {
      expect(5 - 3).toEqual(2);
    });
  });
});
